name: dotnet-implementation
version: 0.2.0
description: >-
  Additive .NET solution scaffolding around the core Microsoft Agent Framework
  (see the `maf-core` package) — bootstrapping a solution and NuGet packages,
  hosting for local development (Aspire AppHost + DevUI, ASP.NET Core self-hosting),
  C# code-execution sandboxing, and the builder agent that drives the end-to-end
  build. The MAF implementation patterns, workflows, and MCP skills live in `maf-core`.
author: code-first-agent-starter
includes: auto
dependencies:
  apm: []
  mcp: []
scripts: {}
